## Setup
Ensure the Python interpreter is being used

1. Install dependencies:
    pip install -r requirements.txt

2. Run Jupyter Book:
    jupyter-book build .

3. Run the Jupyter Book Server:
    python -m http.server --directory _build/html

4. Open the localhost server by typing into a web browser:
    http://localhost:8000